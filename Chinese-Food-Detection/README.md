# Pytorch环境安装
```
$ conda create -n IAT python==3.11
$ conda activate IAT
$ conda install pytorch torchvision torchaudio pytorch-cuda -c pytorch -c nvidia
$ pip install xlwt matplotlib tqdm -i https://pypi.tuna.tsinghua.edu.cn/simple
```
